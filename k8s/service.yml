apiVersion: v1
kind: Service
metadata:
  name: stockalert-service
  namespace: stockalert
spec:
  type: NodePort
  selector:
    app: stockalert
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080